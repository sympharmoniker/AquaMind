# Python
__pycache__/
*.pyc
*.pyo

# 機敏設定(本機保留,不進 git)
analysis/config.py
analysis/.streamlit/secrets.toml

# Pi 端環境變數(API key 等),只允許範本進 git
aquamind_config.env
setup/aquamind_config.env
!setup/aquamind_config.env.example

# 系統檔
.DS_Store
Thumbs.db

# Word / Excel 草稿(本機保留,不進 git)
*.docx
*.xlsx

# ML 模型 pickle 檔(各機器各自訓練,但 dashboard 用的 anomaly_model.pkl 例外推進 git)
*.pkl
!analysis/anomaly_model.pkl

# 本機測試資料夾(下載 CSV 用,不進 git)
第二次數據測試*/
測試資料/
